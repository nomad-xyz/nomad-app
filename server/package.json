{
  "name": "bridge-web-app",
  "version": "1.4.7",
  "private": true,
  "scripts": {
    "any": "ts-node server/index",
    "prisma-build": "prisma generate",
    "docker-build": "docker-compose build",
    "migrate": "prisma db push",
    "start": "npm run migrate && npm run any"
  },
  "dependencies": {
    "@prisma/client": "^3.9.1",
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "prisma": "^3.9.1",
    "ts-node": "^10.7.0"
  }
}
